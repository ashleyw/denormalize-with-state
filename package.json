{
  "name": "denormalize-with-state",
  "version": "0.3.1",
  "description": "denormalize-with-state takes data denormalized by denormalizr and merges in extra state.",
  "keywords": [
    "normalizr",
    "normalize",
    "denormalizr",
    "denormalize"
  ],
  "files": [
    "lib"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ashleyw/denormalize-with-state.git"
  },
  "homepage": "https://github.com/ashleyw/denormalize-with-state#readme",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha --compilers js:babel-core/register --grep performance --invert test/tests",
    "test:performance": "mocha --compilers js:babel-core/register --grep performance test/tests",
    "test:all": "mocha --compilers js:babel-core/register test/tests",
    "build": "babel src --out-dir lib",
    "prepublish": "npm run build",
    "lint": "eslint src test"
  },
  "author": "Ashley Williams",
  "license": "MIT",
  "dependencies": {
    "denormalizr": "^0.4.1"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-eslint": "^7.1.1",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.18.0",
    "benchmark": "^2.1.3",
    "chai": "^3.5.0",
    "eslint": "^3.13.1",
    "eslint-config-airbnb-base": "^11.0.1",
    "eslint-plugin-flowtype": "^2.30.0",
    "eslint-plugin-import": "^2.2.0",
    "flow-bin": "^0.38.0",
    "mocha": "^3.2.0",
    "normalizr": "^2.0.0",
    "performance-now": "^2.0.0"
  }
}
